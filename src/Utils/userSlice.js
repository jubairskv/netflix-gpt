const userSlice = createSlice({
    name:"user",
    initialState:null,
    reducers:{
        addUser:{state, action.payload} =>{

        },
        removeUser:{state, action.payload} =>{

        }
    }
})